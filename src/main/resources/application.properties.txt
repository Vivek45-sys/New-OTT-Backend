# Expose actuator endpoints
management.endpoints.web.exposure.include=health,prometheus
management.endpoint.prometheus.enabled=true
management.server.port=8081  # optional if you want separate port
